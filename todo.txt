- Connexion remettre les champs par défaut (vide) après utilisateurs existe déjà -- FAIT

- User connexion --> Emmene vers une page ou sera deco -- FAIT

- Admin comment faire pour le role
    No token for the admin (in security purpose)

- Admin will have different view

- Token quand connexion (1 jours)

- Gerer les logs

- Crypter mdp -- FAIT

- Déconnexion --> supp le token

- Command line argument in order to instantiate the database with the right schema from the db.json that will
    contain the schema of the database and the data that will be inserted in the database
- pas sur mais automatisé l'implémentaion de la base de donnée

- Ameliorer le view en general

- Manage the errors : function that take argument (mail,pwd,..) explaining the error
    verify the mail is an mail, verify the password with restriction (6 characters...) -- FAIT mais améliorer le view et ajouter des erreurs si nécessaire

- Add a security about password tentative (5 tentative max per 10 minutes)

- Create unitary test -- FAIT Mais dev ceux du loginerrorhandler

- Delete user when connected by creating a button (check if wallet equal to 0)

- Add profile modification when connected (mail, password, name)